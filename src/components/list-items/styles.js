import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    marginBottom: 73,
    marginHorizontal: 10,
    paddingVertical: 10,
  },
});

export default styles;
